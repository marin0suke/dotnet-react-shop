import { Box, Typography } from '@mui/material';

const CampaignsPage = () => {
  return (
    <Box p={4}>
      <Typography variant="h4">Marketing Campaigns</Typography>
      <Typography color="text.secondary">
        Launch promotional banners or discount campaigns.
      </Typography>
    </Box>
  );
};

export default CampaignsPage; 